{
  "name": "wedding",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Wedding planning board for two people and their planners, backed by a Google Sheet.",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/build-sw.js",
    "preview": "vite preview",
    "icons": "node scripts/make-icons.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.0.0",
    "vite": "^7.0.0",
    "vitest": "^3.2.0"
  }
}
